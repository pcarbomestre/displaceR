% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario.R
\name{read_displace_scenario}
\alias{read_displace_scenario}
\title{Read a DISPLACE scenario file}
\usage{
read_displace_scenario(
  input_dir = NULL,
  input_name = NULL,
  scenario = "baseline",
  path = NULL
)
}
\arguments{
\item{input_dir}{Folder containing the `simusspe_*` subfolder.}

\item{input_name}{Parameterisation name (DISPLACE's `-f`).}

\item{scenario}{Scenario name (DISPLACE's `-F`).}

\item{path}{Read this exact file instead.}
}
\value{
An object of class `displace_scenario`.
}
\description{
Reads `<input_dir>/simusspe_<input_name>/<scenario>.dat`. This is the file
DISPLACE's `-F` selects, and `baseline.dat` is the default one upstream
ships.
}
\details{
It carries `nrow_coord` and `nrow_graph`, which are the row counts the
stacked-column graph files in `graphsspe/` are parsed with. A scenario and
its graph must therefore agree, and [read_displace_graph()] takes those
counts from here.
}
\examples{
f <- tempfile(fileext = ".dat")
write_displace_scenario(new_displace_scenario(nrow_coord = 10, nrow_graph = 20),
                        path = f)
read_displace_scenario(path = f)
}
