% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph.R
\name{write_displace_graph}
\alias{write_displace_graph}
\title{Write the DISPLACE spatial graph}
\usage{
write_displace_graph(graph, input_dir, a_graph = NULL, code_area = NULL)
}
\arguments{
\item{graph}{A `displace_graph`, or a list with `nodes` and `edges` data
frames using the same column names [read_displace_graph()] returns.}

\item{input_dir}{Folder to write `graphsspe/` into.}

\item{a_graph}{Graph number. Defaults to the graph's own.}

\item{code_area}{Optional integer vector of area codes, one per node. When
given, `code_area_for_graph<N>_points.dat` is written too. The simulator
reads and discards the first two blocks of that file, so they are filled
with zeros.}
}
\value{
The paths written, invisibly.
}
\description{
Emits `coord<N>.dat` and `graph<N>.dat` in the column-stacked layout the
simulator expects.
}
\examples{
g <- list(
  nodes = data.frame(node_id = 0:2, lon = c(10, 11, 12),
                     lat = c(55, 56, 57), harbour = c(1L, 0L, 0L)),
  edges = data.frame(from = c(0L, 1L), to = c(1L, 2L), dist_km = c(3.2, 4.8))
)
write_displace_graph(g, tempdir(), a_graph = 1)
}
