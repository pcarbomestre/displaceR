% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{displace_versions}
\alias{displace_versions}
\alias{displace_default_version}
\title{DISPLACE binary versions known to this package}
\usage{
displace_versions()

displace_default_version()
}
\value{
A data frame with one row per version, or a zero-row data frame if
  no binaries have been published yet. Columns: `version`, `upstream_sha`,
  `displace_version`, `released`, `glibc` (comma-separated targets available)
  and `is_default`.

`displace_default_version()` returns the version label
  [install_displace()] uses when none is given, or `NULL` if the manifest
  pins no default.
}
\description{
Lists the entries in the package manifest — the builds that
[install_displace()] can fetch. This is not a list of what is installed; use
[displace_path()] for that.
}
\examples{
displace_versions()
}
