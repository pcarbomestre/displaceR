% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_sqlite.R
\name{displace_db_query}
\alias{displace_db_query}
\title{Run arbitrary SQL against a DISPLACE output database}
\usage{
displace_db_query(x, sql)
}
\arguments{
\item{x}{A `displace_run`, a database path, or an output directory.}

\item{sql}{A `SELECT` statement. The connection is read-only, so anything
that writes will fail.}
}
\value{
A data frame.
}
\description{
The escape hatch for aggregation the helpers do not cover. Aggregating in
SQL is usually much faster than pulling a whole table into R.
}
\examples{
\dontrun{
displace_db_query(res, "
  SELECT tstep, SUM(catches) AS total
  FROM VesselLogLikeCatches GROUP BY tstep ORDER BY tstep
")
}
}
