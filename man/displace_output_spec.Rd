% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formats.R
\name{displace_output_spec}
\alias{displace_output_spec}
\title{Column layout of a DISPLACE text output file}
\usage{
displace_output_spec(
  type,
  nbpops = NULL,
  explicit_pops = NULL,
  db_version = NULL
)
}
\arguments{
\item{type}{Output type, one of `names(displace_output_types())`.}

\item{nbpops}{Number of populations, from `config.dat`. Required for the
variable-width layouts.}

\item{explicit_pops}{Zero-based ids of the explicitly modelled populations,
i.e. `setdiff(0:(nbpops-1), implicit_pops)`. Required for `loglike`.}

\item{db_version}{Output schema version, for future dispatch. Currently
unused: there is one layout in the wild.}
}
\value{
A character vector of column names.
}
\description{
Returns the column names for one of DISPLACE's text output files. Several
layouts are not fixed: their width depends on the number of populations in
the case study, so `nbpops` (and, for `loglike`, `explicit_pops`) must be
supplied.
}
\examples{
displace_output_spec("popstats")
displace_output_spec("loglike", nbpops = 3, explicit_pops = c(0, 2))
}
