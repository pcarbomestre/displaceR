% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_sqlite.R
\name{read_displace_db}
\alias{read_displace_db}
\title{Read a table from a DISPLACE output database}
\usage{
read_displace_db(x, table, where = NULL, limit = NULL, check_version = TRUE)
}
\arguments{
\item{x}{A `displace_run`, a path to a `*_out.db`, or a run output directory.}

\item{table}{Table name. See [DISPLACE_DB_TABLES] for the known set, or call
[displace_db_tables()] on an actual database.}

\item{where}{Optional SQL `WHERE` clause, without the keyword. Use this
rather than reading everything and subsetting in R: `VesselVmsLike` on a
multi-year run has tens of millions of rows.}

\item{limit}{Optional row limit.}

\item{check_version}{Warn if the database's schema version is newer than the
one this package was checked against.}
}
\value{
A data frame.
}
\description{
Read a table from a DISPLACE output database
}
\examples{
\dontrun{
read_displace_db(res, "VesselLogLike", where = "tstep < 1000")
}
}
