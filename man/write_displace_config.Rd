% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{write_displace_config}
\alias{write_displace_config}
\title{Write a DISPLACE `config.dat`}
\usage{
write_displace_config(config, input_dir = NULL, input_name = NULL, path = NULL)
}
\arguments{
\item{config}{A `displace_config`, from [new_displace_config()] or
[read_displace_config()].}

\item{input_dir, input_name}{Destination, as in [read_displace_config()].}

\item{path}{Write to this exact file instead.}
}
\value{
The path written, invisibly.
}
\description{
Emits the positional layout the simulator expects, with the conventional
comment lines in the gaps so the file stays readable.
}
\details{
The three `calib_*` vectors are checked against `nbpops` first: the
simulator throws on that mismatch at load time, and finding out here is
considerably cheaper.
}
\examples{
cfg <- new_displace_config(nbpops = 2, nbmets = 4)
write_displace_config(cfg, path = tempfile())
}
